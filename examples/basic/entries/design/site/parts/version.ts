/**
 * The package version the mockup depicts. The real site reads this field from
 * the workspace `@mokly/mokly` package at build time; the mockup records the
 * released version at the time of authoring, matching the changelog entry it
 * shows, because an entry module must not reach outside the example source.
 */

/** The published `@mokly/mokly` version depicted in the chrome. */
export const PACKAGE_VERSION = "0.9.0";
